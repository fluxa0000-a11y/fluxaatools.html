<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fluxaa - Smart Digital Tools</title>
    <style>
        /* CSS RESET & THEME DESIGN */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            -webkit-tap-highlight-color: transparent;
        }

        body {
            background-color: #070a13;
            color: #ffffff;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            overflow-x: hidden;
            position: relative;
        }

        /* 1. SPLASH SCREEN (MODIFIED TEXT) */
        #splash-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: #070a13;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            transition: opacity 0.5s ease-out;
            text-align: center;
            padding: 20px;
        }

        .splash-title {
            font-size: 2.2rem;
            font-weight: 800;
            background: linear-gradient(45deg, #00d2ff, #9a4eff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: 1px;
            margin-bottom: 10px;
            text-transform: uppercase;
        }

        .splash-sub {
            color: #707e94;
            font-size: 0.95rem;
            font-weight: 500;
            letter-spacing: 2px;
        }

        /* SCREEN VISIBILITY UTILITY */
        .app-screen {
            display: none;
            flex-direction: column;
            align-items: center;
            padding: 20px;
            width: 100%;
            max-width: 500px;
            margin: 0 auto;
            flex: 1;
        }

        .active-screen {
            display: flex !important;
        }

        /* GLOBAL COMPONENTS */
        .brand-header {
            font-size: 2rem;
            font-weight: bold;
            color: #00d2ff;
            margin-bottom: 20px;
            text-align: center;
        }

        .primary-btn {
            width: 100%;
            background: linear-gradient(45deg, #00d2ff, #9a4eff);
            color: white;
            border: none;
            padding: 14px;
            border-radius: 12px;
            font-size: 1rem;
            font-weight: bold;
            cursor: pointer;
            box-shadow: 0 4px 15px rgba(154, 78, 255, 0.3);
            transition: 0.3s;
            margin-top: 15px;
        }

        .primary-btn:hover {
            transform: translateY(-2px);
            opacity: 0.95;
        }

        .secondary-btn {
            width: 100%;
            background: #141b30;
            border: 1px solid #1e2942;
            color: #fff;
            padding: 14px;
            border-radius: 12px;
            font-size: 1rem;
            font-weight: bold;
            cursor: pointer;
            margin-top: 10px;
            transition: 0.3s;
        }

        /* HOME SCREEN STYLE */
        .home-card {
            background: #0d1222;
            border: 1px solid #1e2942;
            border-radius: 20px;
            padding: 25px;
            text-align: center;
            margin-top: 20px;
            width: 100%;
        }

        .home-desc {
            color: #a0aec0;
            font-size: 1rem;
            line-height: 1.6;
            margin-bottom: 15px;
        }

        .badge-free {
            display: inline-block;
            background: rgba(0, 210, 255, 0.1);
            color: #00d2ff;
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
            border: 1px solid rgba(0, 210, 255, 0.3);
            margin-bottom: 20px;
        }

        /* BEFORE / AFTER DEMO GRAPHIC */
        .demo-box {
            position: relative;
            width: 100%;
            height: 200px;
            background: #101626;
            border-radius: 15px;
            overflow: hidden;
            border: 1px solid #1e2942;
            margin-bottom: 20px;
            display: flex;
        }

        .demo-side {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            font-weight: bold;
            font-size: 1.2rem;
            position: relative;
        }

        .demo-before {
            background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('https://images.unsplash.com/photo-1506744038136-46273834b3fb?w=100') center/cover;
            filter: blur(4px) grayscale(50%);
            color: #ff4a4a;
        }

        .demo-after {
            background: url('https://images.unsplash.com/photo-1506744038136-46273834b3fb?w=500') center/cover;
            color: #00d2ff;
            border-left: 2px solid #fff;
        }

        .demo-label {
            position: absolute;
            background: rgba(0,0,0,0.7);
            padding: 4px 8px;
            border-radius: 6px;
            font-size: 0.75rem;
            bottom: 10px;
        }

        /* IMAGE UPLOAD & PREVIEW ELEMENTS */
        .image-preview-container {
            width: 100%;
            background: #0d1222;
            border: 1px solid #1e2942;
            border-radius: 20px;
            padding: 20px;
            text-align: center;
        }

        .view-frame {
            width: 100%;
            max-height: 300px;
            object-fit: contain;
            border-radius: 10px;
            border: 1px solid #1e2942;
            margin-top: 15px;
        }

        /* GENERATING LOADING STATE */
        .loader-spinner {
            border: 4px solid #141b30;
            border-top: 4px solid #9a4eff;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            animation: spin 1s linear infinite;
            margin-bottom: 20px;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* GLOBAL FOOTER (MODIFIED EXACT LAYOUT) */
        footer {
            width: 100%;
            background: #070a13;
            border-top: 1px solid #141b30;
            padding: 30px 20px;
            text-align: center;
            font-size: 0.95rem;
            color: #707e94;
            margin-top: auto;
        }

        .footer-info {
            font-size: 1.05rem;
            color: #a0aec0;
            margin-bottom: 8px;
        }

        .footer-link-btn {
            color: #00d2ff;
            text-decoration: none;
            font-weight: 600;
            border-bottom: 1px dotted #00d2ff;
        }

        .footer-copyright {
            margin-top: 15px;
            font-size: 0.9rem;
            color: #515f73;
        }

        .footer-copyright strong {
            color: #a0aec0;
        }
    </style>
</head>
<body>

    <div id="splash-screen">
        <div class="splash-title">Image to FHD Image</div>
        <div class="splash-sub">Powered by Fluxaa</div>
    </div>

    <div id="home-screen" class="app-screen active-screen">
        <div class="brand-header">Fluxaa Studio</div>
        <div class="home-card">
            <p class="home-desc">Convert your low-resolution, blurry photos into crisp, crystal clear Ultra Full HD images instantly inside your device.</p>
            <span class="badge-free">⚡ Unlimited Free Access</span>
            <button class="primary-btn" onclick="navigateTo('workspace-screen')">Try Now</button>
        </div>
    </div>

    <div id="workspace-screen" class="app-screen">
        <div class="brand-header">Fluxaa Studio</div>
        
        <div class="demo-box">
            <div class="demo-side demo-before"><span class="demo-label" style="left:10px;">BEFORE</span></div>
            <div class="demo-side demo-after"><span class="demo-label" style="right:10px;">AFTER (FHD)</span></div>
        </div>

        <div class="image-preview-container">
            <input type="file" id="photo-picker" accept="image/*" style="display: none;">
            <button class="secondary-btn" id="select-btn">Select Photo</button>
            
            <img id="user-photo-view" class="view-frame" style="display: none;">
            <button class="primary-btn" id="fhd-convert-btn" style="display: none;">Change to FHD</button>
        </div>
    </div>

    <div id="processing-screen" class="app-screen">
        <div class="brand-header">Processing Pixels</div>
        <div style="display: flex; flex-direction: column; align-items: center; justify-content: center; flex: 1; margin-top: 60px;">
            <div class="loader-spinner"></div>
            <h3 style="color: #9a4eff; letter-spacing: 1px;">Generating......</h3>
            <p style="color: #515f73; font-size: 0.85rem; margin-top: 10px;">Enhancing matrix grid optimization</p>
        </div>
    </div>

    <div id="result-screen" class="app-screen">
        <div class="brand-header">FHD Generation Ready</div>
        <div class="image-preview-container">
            <img id="final-fhd-view" class="view-frame">
            <button class="primary-btn" id="download-final-btn">Download Image</button>
            <button class="secondary-btn" id="replace-btn">Replace Photo</button>
        </div>
    </div>

    <footer>
        <div class="footer-info">For More Information About Fluxaa</div>
        <a href="https://fluxaaofficial.vercel.app" target="_blank" class="footer-link-btn">Click Here 🚀</a>
        <div class="footer-copyright">
            © 2026 <strong>Fluxaa Technologies</strong>. All rights reserved.
        </div>
    </footer>

    <canvas id="hidden-engine-canvas" style="display: none;"></canvas>

    <script>
        // DOM Handles
        const pickerInput = document.getElementById('photo-picker');
        const selectBtn = document.getElementById('select-btn');
        const userPhotoView = document.getElementById('user-photo-view');
        const fhdConvertBtn = document.getElementById('fhd-convert-btn');
        const finalFhdView = document.getElementById('final-fhd-view');
        const downloadFinalBtn = document.getElementById('download-final-btn');
        const replaceBtn = document.getElementById('replace-btn');
        
        const canvas = document.getElementById('hidden-engine-canvas');
        const ctx = canvas.getContext('2d');
        
        let targetRawData = null;
        let finalProductOutput = null;

        // 1. AUTO SPLASH SCREEN CONTROLLER
        window.addEventListener('DOMContentLoaded', () => {
            // Push initial home state history token to intercept back key
            history.pushState({ screen: 'home-screen' }, '');
            
            setTimeout(() => {
                const splash = document.getElementById('splash-screen');
                splash.style.opacity = '0';
                setTimeout(() => splash.style.display = 'none', 500);
            }, 3000); 
        });

        // 2. HARD-WIRED SINGLE PAGE BACK BUTTON ROUTING INTERCEPTOR
        window.addEventListener('popstate', function(event) {
            if (event.state && event.state.screen) {
                // System dynamic fallback tracking state trigger
                executeNavigationLogic(event.state.screen, false);
            } else {
                // Default fallback tracking router anchor protection
                executeNavigationLogic('home-screen', false);
                history.pushState({ screen: 'home-screen' }, '');
            }
        });

        function navigateTo(screenId) {
            // Push layout stack trace parameters on native router configuration array
            history.pushState({ screen: screenId }, '');
            executeNavigationLogic(screenId, true);
        }

        function executeNavigationLogic(screenId, shouldScroll) {
            document.querySelectorAll('.app-screen').forEach(screen => {
                screen.classList.remove('active-screen');
            });
            document.getElementById(screenId).classList.add('active-screen');
            if(shouldScroll) window.scrollTo(0, 0);
        }

        // TRIGGER NATIVE FILE PICKER
        selectBtn.addEventListener('click', () => pickerInput.click());

        // HANDLING IMAGE SELECTION
        pickerInput.addEventListener('change', function(e) {
            const file = e.target.files[0];
            if (!file) return;

            const reader = new FileReader();
            reader.onload = function(event) {
                targetRawData = event.target.result;
                userPhotoView.src = targetRawData;
                userPhotoView.style.display = 'block';
                fhdConvertBtn.style.display = 'block';
                selectBtn.innerText = "Change Selection";
            };
            reader.readAsDataURL(file);
        });

        // CONVERT TO FHD PROCESS ROUTE (WITH LOGIC EMULATION)
        fhdConvertBtn.addEventListener('click', () => {
            if(!targetRawData) return;
            
            navigateTo('processing-screen');

            const img = new Image();
            img.onload = function() {
                const hdMultiplier = 3;
                canvas.width = img.width * hdMultiplier;
                canvas.height = img.height * hdMultiplier;
                
                ctx.imageSmoothingEnabled = true;
                ctx.imageSmoothingQuality = 'high';
                ctx.drawImage(img, 0, 0, canvas.width, canvas.height);
                
                ctx.filter = "contrast(1.12) saturate(1.04) brightness(1.01)";
                ctx.drawImage(canvas, 0, 0);
                ctx.filter = "none";
                
                // Direct Base64 asset configuration binding profile arrays
                finalProductOutput = canvas.toDataURL('image/jpeg', 1.0);

                setTimeout(() => {
                    finalFhdView.src = finalProductOutput;
                    navigateTo('result-screen');
                }, 4500); 
            };
            img.src = targetRawData;
        });

        // 3. WORKABLE NATIVE DOWNLOADING ARCHITECTURE CORE
downloadFinalBtn.addEventListener('click', () => {
    if (!finalProductOutput) return;
    
    try {
        // 1. Base64 data ko binary bytes mein convert karna
        const byteString = atob(finalProductOutput.split(',')[1]);
        const mimeString = finalProductOutput.split(',')[0].split(':')[1].split(';')[0];
        const ab = new ArrayBuffer(byteString.length);
        const ia = new Uint8Array(ab);
        
        for (let i = 0; i < byteString.length; i++) {
            ia[i] = byteString.charCodeAt(i);
        }
        
        // 2. Real File (Blob) create karna
        const blob = new Blob([ab], { type: mimeString });
        const blobUrl = URL.createObjectURL(blob);
        
        // 3. Native Hidden Anchor method
        const nativeDownloadAnchor = document.createElement('a');
        nativeDownloadAnchor.download = `fluxaa-fhd-${Date.now()}.jpg`;
        nativeDownloadAnchor.href = blobUrl;
        
        document.body.appendChild(nativeDownloadAnchor);
        nativeDownloadAnchor.click();
        
        // Safety Clean up
        setTimeout(() => {
            document.body.removeChild(nativeDownloadAnchor);
            URL.revokeObjectURL(blobUrl);
        }, 100);

    } catch (error) {
        // 4. FALLBACK: Agar mobile security download fail kare, toh safe side photo open kar do
        console.log("Mobile redirect fallback active");
        const newTab = window.open();
        if (newTab) {
            newTab.document.write(`<h3 style="color:#00d2ff; text-align:center; font-family:sans-serif; margin-top:20px;">Fluxaa HD Studio</h3><p style="color:#666; text-align:center; font-family:sans-serif;">Photo par Long Press (daba kar) rakhein aur "Save Image" select karein.</p><img src="${finalProductOutput}" style="max-width:100%; height:auto; display:block; margin:20px auto; border-radius:10px; box-shadow:0 4px 10px rgba(0,0,0,0.3);"/>`);
        } else {
            // Agar pop-up bhi block ho jaye, toh direct window replace
            window.location.href = finalProductOutput;
        }
    }
});

        // REPLACE BUTTON ACTION ROUTE
        replaceBtn.addEventListener('click', () => {
            targetRawData = null;
            finalProductOutput = null;
            userPhotoView.src = "";
            userPhotoView.style.display = 'none';
            fhdConvertBtn.style.display = 'none';
            selectBtn.innerText = "Select Photo";
            pickerInput.value = "";
            
            navigateTo('workspace-screen');
        });
    </script>
</body>
</html>
